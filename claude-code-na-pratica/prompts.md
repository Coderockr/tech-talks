// Exemplos de tasks simples:

- Renomeie o componente `@src/components/form/ActionButton` para `SubmitButton` e atualize todas as referências para o novo nome.

- Adicione uma propriedade do tipo número inteiro chamada `unitPrice` na interface `Product` (@src/types/product.ts).

// Exemplos de tasks complexas:

- Implemente um modelo de autenticação baseado em sessões JWT com suporte a refresh token.

- Crie um componente para tabelas e atualize todas as listagens do sistema para utilizar este componente.
